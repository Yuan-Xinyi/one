\documentclass[journal, twoside]{IEEEtran}
\let\labelindent\relax
% math related package
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
% table related package
\usepackage{tabularx}
\usepackage{ulem}
\usepackage{threeparttable}
\usepackage{booktabs}
% figure related package
\usepackage{graphicx}
\usepackage{wrapfig}
% algorithm related package
% \usepackage[lined, ruled, linesnumbered, comsmentsnumbered, noend]{algorithm2e}
\usepackage[lined, ruled, linesnumbered, noend]{algorithm2e}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\renewcommand{\thefootnote}{\tiny~Note \arabic{footnote}}
\newcommand\mycommfont[1]{\footnotesize\ttfamily\textcolor{blue}{#1}}
\SetCommentSty{mycommfont}
% unknown
\usepackage{subcaption}
\usepackage{eqlist}
\usepackage{amsfonts}
\usepackage{enumitem}
\usepackage{lipsum}
\usepackage{stfloats}
\usepackage[numbers]{natbib}
\normalem
\usepackage{colortbl}
\def\mathbi#1{\textbf{\em #1}}
\usepackage[usenames,dvipsnames]{xcolor}
\usepackage{comment}
% \usepackage{flushend}
\newcommand{\harada}[1]{\textcolor{green}{#1}}
\newcommand{\recheck}[1]{\textcolor{olive}{#1}}
\usepackage{multirow}
\newcommand\dunderline[3][-2pt]{{%
  \setbox0=\hbox{#3}
  \ooalign{\copy0\cr\rule[\dimexpr#1-#2\relax]{\wd0}{#2}}}}
\newcommand{\rev}[1]{\textcolor{magenta}{#1}}
\hyphenation{}
\usepackage{tikz}
\usetikzlibrary{fit,shapes.misc}
\newcommand\marktopleft[1]{%
    \tikz[overlay,remember picture]
        \node (marker-#1-a) at (0,1.5ex) {};%
}
\newcommand\markbottomright[1]{%
    \tikz[overlay,remember picture]
        \node (marker-#1-b) at (0,0) {};%
    \tikz[overlay,remember picture,thick,inner sep=3pt]
        \node[draw, rectangle, color=teal, fit=(marker-#1-a.center) (marker-#1-b.center)] {};%
}

\usepackage{CJKutf8}
\definecolor{bleudefrance}{rgb}{0.19, 0.55, 0.91}
\definecolor{awesome}{rgb}{1.0, 0.13, 0.32}
\newcommand{\wan}[1]{{\color{awesome}\begin{CJK*}{UTF8}{gkai}\small#1\end{CJK*}}}
\newcommand{\wanold}[1]{{\color{gray}\begin{CJK*}{UTF8}{gkai}\small#1\end{CJK*}}}
\newcommand{\wanextra}[1]{{\color{blue}\begin{CJK*}{UTF8}{gkai}\small#1\end{CJK*}}}

\definecolor{darkgreen}{rgb}{0.0, 0.65, 0.0}
\definecolor{babyblue}{rgb}{0.29, 0.75, 0.93}
\newcommand{\tang}[1]{{\color{darkgreen}\begin{CJK*}{UTF8}{gkai}\small#1\end{CJK*}}}
\newcommand{\junboextra}[1]{{\color{babyblue}\begin{CJK*}{UTF8}{gkai}\small#1\end{CJK*}}}

\newcommand{\lqin}[1]{{\color{babyblue}\begin{CJK*}{UTF8}{gkai}\small#1\end{CJK*}}}

\newcommand{\limebox}[1]{\setlength{\fboxsep}{0pt}{\colorbox{lime}{#1}}}
\newcommand{\pinkbox}[1]{\setlength{\fboxsep}{0pt}{\colorbox{yellow}{#1}}}

%new colors
\definecolor{darkorange}{rgb}{1.0, 0.55, 0.0}
\newcommand{\xinyi}[1]{{\color{darkorange}\begin{CJK*}{UTF8}{gkai}#1\end{CJK*}}}
\newcommand{\xinyitodo}[1]{{\color{darkgreen}\begin{CJK*}{UTF8}{gkai}#1\end{CJK*}}}
% Text written in this revision pass; existing prose is left untouched.
\newcommand{\new}[1]{\textcolor{red}{#1}}
\newtheorem{proposition}{Proposition}
\newtheorem{remark}{Remark}

\begin{document}
% \title{Extreme Motion Generation via Hybrid Null-Space Control for Straight-Line Path Following}
% Exploiting Continuous Kinematic Reachability \\for Generating Extreme Linear Motion in Workspace
\title{Extreme Motion Generation via \\Velocity-Limited Null-Space Actions}

% two-dimensional manifolds
% \author{Yu Tang$^{1}$, Weiwei Wan$^{*1}$, Hao Chen$^{2}$, Masaki Matsushita$^{3}$, Jun Takahashi$^{3}$, Takeyuki Kotaka$^{3}$, Kensuke Harada$^{1}$
% \thanks{1. Graduate School of Engineering Science, Osaka University, Japan. 2. College of Computer and Information Sciences, Fujian Agriculture and Forestry University, China. 3. H.U. Group Research Institute G.K., Japan}
% \thanks{Contact: Weiwei Wan, {\tt\small wan@sys.es.osaka-u.ac.jp}}}
% \markboth{Under review by a robotics journal}
% {Tang \MakeLowercase{\textit{et al.}}: Zero-Shot Test Tube Recognition}

\author{Authors' names have been anonymized following the IEEE RAS Double-Anonymous Review Process Guidelines
\thanks{Authors' affiliations and contact information have been anonymized following the IEEE RAS Double-Anonymous Review Process Guidelines.}}
\markboth{IEEE Robotics and Automation Letters. Submission for Review, 2026.}
{Authors' names anonymized: Exploiting Continuous Kinematic Reachability}
\maketitle

\bstctlcite{IEEEexample:BSTcontrol}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{abstract}
This work studies \textit{extreme motion generation}, which aims to maximize the Cartesian path length along a pre-defined trajectory within the manipulator's workspace. Conventional reachability analysis focuses on pointwise attainability, without considering whether the start and goal can be connected by a continuous, constraint-satisfying trajectory in the workspace. This objective is particularly important for industrial tasks such as welding and surface coating, which require long, uninterrupted strokes. However, this objective is challenging for both classical and learning-based approaches, as maximizing future returns may require deliberately approaching the safety boundary for more favorable future configurations. Accordingly, we leverage reinforcement learning for long-horizon decision-making and define the action space in terms of velocity-limited null-space motions. The initial joint configuration is selected through supervised learning from CVT-based candidate batches. We evaluate the proposed framework across three manipulators on four spatial path-following tasks in simulation and real-world experiments. Relative to the pointwise-reachable reference, the proposed method recovers X\% of the reference path length and consistently outperforms classical baselines. The method further provides practical guidance for task-aware base placement.


\end{abstract}

\begin{IEEEkeywords}
Extreme Motion Generation, Kinematic Reachability, Reinforcement Learning
\end{IEEEkeywords}

%==============================================================================================================================
\section{Introduction}

\begin{figure*}[!tbp]
\centering
\includegraphics[width=\linewidth]{imgs/motivation_fig.JPG}
\caption{Motivation of extreme motion generation. (a) Proper initial configuration and redundancy resolution enable the door to open to $90^\circ$. (b,c) Poor initial configurations reduce the achievable opening angle, while (d,e) poor redundancy-resolution strategies lead to early termination.}
\label{fig:pwgap}
\end{figure*}

\IEEEPARstart{C}{onsider} a compact, fixed-base manipulator executing a coating or welding operation over a large work surface. Given a starting position and a motion direction, intuition would suggest that the maximum reachable point is determined by the reachable workspace. However, the motion is continuous and strictly constrained by safety boundaries such as joint limits and tool-orientation tolerance along each waypoint. Therefore, the practical bottleneck is that one of those safety constraints is violated and causes termination long before the end-effector approaches the edge of the reachable workspace. Consequently, the practically achievable path length is bounded by the maximum pointwise-reachable length, depending on how long boundary violation can be deferred during the motion execution. We refer to this problem as \textit{extreme motion generation} that maximizes the Cartesian path length along a pre-defined trajectory within the manipulator's workspace. This is particularly important in industrial automation, where the length of a single continuous motion directly limits the area a manipulator can process in one setup. Rather than scaling up the manipulator or repeatedly relocating the base, simply exploiting the extreme motion offers a solution to enlarge the effective working range.

However, extreme motion generation, especially under task-constrained path-following, is challenging in practice. A joint motion feasible at the current step may gradually steer the subsequent motion toward a safety boundary and lead to termination due to the cumulative effect. Therefore, resolving this issue requires long-horizon decision-making, potentially requiring deliberate approaches to safety boundaries in exchange for greater future returns. However, existing motion generation methods struggle to balance such long-horizon exploitation with robust constraint adherence. Classical controllers optimize instantaneous secondary objectives while realizing the desired end-effector motion. Although robust and predictable near boundaries, they are inherently myopic and overlook long-horizon consequences through local optimization. In contrast, learning-based methods can capture complex state-action distributions, but their performance deteriorates sharply near safety boundaries due to sparse data coverage.

We carefully investigate extreme motion generation and identify two factors that determine the maximum stroke length: initial joint configuration selection and real-time redundancy resolution. The former is a one-time decision that can be learned through supervised learning, whereas the latter is more challenging due to the aforementioned dilemma between instantaneous margins and long-horizon foresight. Therefore, we formulate real-time redundancy resolution as an exit-time optimal control problem and leverage reinforcement learning for long-horizon decision-making. Specifically, we restrict the policy to the null space of the position Jacobian, isolating redundancy resolution from task-space tracking. Meanwhile, we constrain the output action by the residual joint velocity after accounting for task-space motion, thereby ensuring physical feasibility. More importantly, the reward function is aligned directly with the exit-time objective, without shaping terms that may induce greedy behavior.

The proposed framework has been evaluated across three manipulators on $10{,}000$ unseen straight-line path-following tasks and $7{,}500$ unseen and Out-Of-Distribution curve tasks.\xinyi{写到这里了。}

We evaluate the proposed framework on a large set of straight-line path-following tasks for a 7-DoF Franka FR3, measuring performance as the achieved path length relative to a per-task reference length. The contribution of each component is then analyzed through the seed-by-controller protocol. For the initial joint configuration, the diffusion-based selection is compared against several hand-designed heuristic seeds; for the controller, the hybrid framework is compared against the classical and the learning-based controllers in isolation. We further conduct ablation studies on the guidance scale, the number of diffusion sampling steps, and the hysteresis switching thresholds, providing practical insights into the effect of each design choice.

In summary, the main contributions of this work are as follows.

\xinyi{
\begin{itemize}
\item We formulate extreme motion generation as an exit-time optimal control problem that maximizes the length of a straight-line workspace trajectory subject to task constraints.

\item We conclude that the maximum stroke depends on both initial configuration selection and real-time redundancy resolution. The proposed framework addresses these two factors through supervised learning and reinforcement learning, respectively.

\item The proposed framework exhibits superior performance across three manipulators on four spatial path-following tasks, generating trajectories that remain within joint-position and joint-velocity limits throughout the motion.
\end{itemize}
}

The rest of this article is organized as follows. Section~II reviews related work. Section~III formulates the problem. Section~IV presents the framework. Section~V reports the experiments, and Section~VI concludes the paper.

\section{Related Work}

\subsection{Reachability and Global Redundancy Resolution}

Manipulability characterizes local dexterity through the
Jacobian~\cite{yoshikawa1985manipulability,yoshikawa1985dynamic,chiu1988task,klein1987dexterity}
and has been incorporated into null-space
resolution~\cite{liegeois1977automatic,jin2017manipulability,jaquier2021geometry,haviland2021neo}
and planning~\cite{shen2023adaptive,cai2025just}. Workspace reachability
instead characterizes global feasibility through capability
maps~\cite{yang2009placement,zacharias2007capturing}, with applications to
base placement~\cite{makhal2018reuleaux,vahrenkamp2013robot} and learned
reachability priors~\cite{feng2025predictive,jauhri2022robot,selim2022safe}.
Neither directly determines how far a prescribed path can be followed by
one continuous joint trajectory.

The global structure underlying this problem has been studied extensively.
Redundant IK solutions form self-motion
manifolds~\cite{burdick1989inverse}, joint limits can divide feasible
solutions into disconnected branches~\cite{shimizu2008analytical,luck1993self},
and global redundancy resolution has been formulated through continuous
pseudoinversion~\cite{hauser2018global} and dynamic programming across
solution classes~\cite{ferrentino2020optimal,ferrentino2021globally,fabregat2025topological}.
Related studies distinguish local and globally optimal redundancy
resolution~\cite{martin1989resolution,suh1987local,kazerounian1988global}.
These methods optimize how a prescribed path is executed, whereas we ask
how far the path remains continuously executable and how this extent
depends on the initial configuration and the subsequent redundancy-resolution
law.

\subsection{Continuous Path Feasibility and Maximum Executable Extent}

Prior work has also studied whether a prescribed path admits a continuous
joint-space realization. This includes the distinction between pointwise,
pathwise, and global resolution~\cite{hauser2016continuous}, cases in
which individually reachable waypoints admit no continuous global
resolution~\cite{hauser2018global}, and connected feasible regions in the
cuspidal-manipulator literature~\cite{wenger1991uniqueness,wenger2023cuspidal,elias2025cuspidal}.
These works primarily characterize feasibility or connectivity rather than
the maximum continuously executable path extent.

A related line of work minimizes reconfigurations or breakpoints when a
complete path cannot be executed continuously~\cite{wang2024iklink,yang2022optimal,yin2024breakpoints}.
Such formulations allow interrupted execution and therefore address a
different objective from maximizing the length of a \emph{single}
continuous stroke. Closest in spirit, Gautier et
al.~\cite{gautier2024comparison} reported maximum weldable extents along
prescribed trajectories for robot morphology and base-placement analysis.
Our focus is instead a task-level characterization of continuous
executable extent relative to pointwise reachability, together with the
roles of initial-configuration selection and redundancy resolution in
realizing that extent.

\subsection{Optimal and Learned Redundancy Control}

When redundancy resolution is judged by future feasible progress rather
than an instantaneous secondary objective, it naturally admits an optimal
control formulation. Classical time-optimal path-following methods show
that control-affine systems with bounded inputs often yield
boundary-valued optimal controls, except on singular
arcs~\cite{bobrow1985time,shin1985minimum}. Boundary-valued structure,
however, is a property of a hand-chosen command box. We parameterize the
null-space command by the exact projector and the manipulator's own
joint-velocity limits, so no such box exists: the amplitude semantics
follow the actuation envelope of the robot rather than a tuned bound.

Learning has also been used for redundancy resolution and inverse
kinematics~\cite{malik2022deep}, while generative and learned IK methods
model or exploit the multiplicity of feasible
configurations~\cite{ames2022ikflow,zhang2025ikdiffuser,huang2024diffusionseeder,yoon2023learning}.
In reinforcement-learning approaches, the deployed policy inherits the
coordinates of its action and observation interface. We show that this
choice, rather than architecture or budget, dominates the achievable
stroke: physically normalizing the interface turns the same learning
algorithm from a mediocre resolution law into the strongest one we
evaluate.


\section{Problem Formulation}

We formulate extreme motion generation as constrained path following with an unknown terminal path extent. We first consider a straight-line task, in which a fixed-base manipulator advances its end-effector along a prescribed ray while satisfying position, orientation, joint-limit, and collision constraints.

Consider an $n$-DoF manipulator with joint configuration $\mathbf{q}\in\mathbb{R}^{n}$. Let $\mathbf{p}(\mathbf{q})\in\mathbb{R}^{3}$ denote the end-effector position and $\mathbf{z}(\mathbf{q})\in\mathbb{R}^{3}$ the TCP local $z$-axis. A task is specified by $\mathbf{c}=[\mathbf{p}_{0},\mathbf{d},\mathbf{n}]\in\mathbb{R}^{9}$, where $\mathbf{p}_{0}$ is the starting position and $\mathbf{d}$ and $\mathbf{n}$ are unit vectors defining the motion direction and task-plane normal. The prescribed path is
\begin{equation}
\mathcal{P}=\{\mathbf{p}_{0}+s\mathbf{d}:s\geq 0\},
\end{equation}
and the end-effector advances along it at commanded speed $v$ with control period $\Delta t$. The formulation below focuses on straight rays; whether the initial-configuration selector, conditioned on the sampled path ahead, transfers to curved path families is evaluated separately in Section~\ref{sec:ood}.

Given an initial configuration $\mathbf{q}_{0}$ and a redundancy-resolution policy $\pi$, execution terminates at the first violation of a feasibility constraint. Let $\mathcal{L}(\mathbf{q}_{0},\pi;\mathbf{c})$ denote the Cartesian length completed before termination. We define the \emph{continuous kinematic reachability} of task $\mathbf{c}$ as
\begin{equation}
\label{eq:ckr}
\mathcal{L}_{\mathrm{CKR}}(\mathbf{c})
=
\sup_{\mathbf{q}_{0},\,\pi}
\mathcal{L}(\mathbf{q}_{0},\pi;\mathbf{c}),
\end{equation}
subject to
\begin{align}
&\operatorname{dist}\!\left(
\mathbf{p}(\mathbf{q}_{t}),\mathcal{P}
\right)
\leq \epsilon_{\mathrm{pos}},
\label{eq:pos_constraint}\\
&\arccos\!\left(
\mathbf{z}(\mathbf{q}_{t})^{\top}\mathbf{n}
\right)
\leq \theta_{\max},
\label{eq:ori_constraint}\\
&\mathbf{q}_{\min}
\preceq \mathbf{q}_{t}
\preceq \mathbf{q}_{\max},
\label{eq:jl_constraint}\\
&\mathbf{q}_{t}\in\mathcal{Q}_{\mathrm{free}},
\label{eq:collision_constraint}
\end{align}
where $\epsilon_{\mathrm{pos}}$ and $\theta_{\max}$ denote the position and orientation tolerances, and $\mathcal{Q}_{\mathrm{free}}$ denotes the collision-free configuration set. In practice, $\mathcal{L}(\mathbf{q}_{0},\pi;\mathbf{c})$ is the stroke realized by a particular initial configuration and controller, whereas $\mathcal{L}_{\mathrm{CKR}}$ is the optimal continuously executable extent.

Because only the $3$-DoF position is tracked, the manipulator retains a null space when the position Jacobian $\mathbf{J}$ has full row rank and $n>3$. The commanded joint velocity is decomposed as
\begin{equation}
\label{eq:nullspace}
\dot{\mathbf{q}}
=
\mathbf{J}^{+}\dot{\mathbf{x}}
+
(\mathbf{I}-\mathbf{J}^{+}\mathbf{J})\,\boldsymbol{\xi},
\end{equation}
where $\mathbf{J}^{+}$ is the pseudoinverse of the position Jacobian and $\boldsymbol{\xi}\in\ker\mathbf{J}$ denotes the secondary joint-space command. Thus, all controllers share the same task-space motion and differ only in how $\boldsymbol{\xi}$ is selected at each control period.

The physical envelope of this command is set by the manipulator's own actuation. Writing $\dot{\mathbf{q}}^{\lim}$ for the per-joint velocity limits, the total command must satisfy the limits componentwise, so the admissible null-space set is
\begin{equation}
\label{eq:uq}
\mathcal{U}(\mathbf{q})
=
\left\{
\boldsymbol{\xi}\in\ker\mathbf{J}:
\left|\mathbf{J}^{+}\dot{\mathbf{x}}+\boldsymbol{\xi}\right|
\preceq
\dot{\mathbf{q}}^{\lim}
\right\},
\end{equation}
a polytope: the axis-aligned velocity box, shifted by the task-tracking term and intersected with the null space. Its extent depends on the configuration and on the direction of $\boldsymbol{\xi}$; the proposed interface exposes this dependence to the policy in closed form (Section~\ref{sec:traj}). Several baselines instead use a conventional parameterization $\boldsymbol{\xi}=\mathbf{B}(\mathbf{q})\mathbf{a}$, where $\mathbf{B}(\mathbf{q})\in\mathbb{R}^{n\times m}$ ($m=n-3$) is an orthonormal task-aligned null-space basis and the coordinate command is box-bounded, $\mathbf{a}\in[-a_{\max},a_{\max}]^{m}$; this construction is specified with the respective controllers in Section~\ref{sec:setup}.

Equation~(\ref{eq:ckr}) therefore contains the two decisions addressed by the proposed framework: selecting the initial configuration $\mathbf{q}_{0}$ and selecting the admissible null-space command $\boldsymbol{\xi}\in\mathcal{U}(\mathbf{q})$ throughout the resulting continuous motion.



\section{Extreme Motion Generation}
\label{sec:emg}

Extreme motion generation contains two coupled decisions. Before execution,
an initial task-feasible configuration must be selected; during execution,
the redundancy must be resolved at every increment. The first stage
generates feasible initial configurations and learns to rank them from
rollout outcomes. The second stage learns a reactive resolution policy
over a physically normalized action interface and deploys it directly,
with no model queries at run time.

\subsection{Exit-Time Optimal Control Formulation}

The stroke terminates at the first constraint violation, so its horizon is
not prescribed in advance. Let $\Omega$ denote the feasible configuration
set defined by the position, orientation, joint-limit, and collision
constraints of Section~III, and define the exit time
\begin{equation}
\label{eq:exit-time}
\tau_{\mathrm{exit}}
=
\inf\{t\geq 0:\mathbf{q}(t)\notin\Omega\}.
\end{equation}
For a fixed task condition $\mathbf{c}$, let $v$ denote the commanded
progress rate along the prescribed path. We define the discounted
exit-time objective as
\begin{equation}
\label{eq:value}
V(\mathbf{q}_{0};\mathbf{c})
=
\sup_{\boldsymbol{\xi}(\cdot)}
\int_{0}^{\tau_{\mathrm{exit}}}
e^{-\beta t}
v\,\mathrm{d}t,
\end{equation}
where $\beta>0$ is the continuous-time discount rate and the supremum is
taken over measurable commands $\boldsymbol{\xi}(\cdot)$ satisfying
$\boldsymbol{\xi}(t)\in\mathcal{U}(\mathbf{q}(t))$. Under the fixed path-advance rate used in this
work, $v$ is constant while the trajectory remains feasible. Hence maximizing
Eq.~(\ref{eq:value}) is equivalent to delaying the first constraint violation
and therefore to maximizing the executed stroke.

This formulation also connects the two stages of the framework. Initial
configuration selection compares the future stroke attainable from
candidate start states, whereas motion generation maximizes the same
objective along the executed trajectory.

\subsection{Initial Joint Configuration Selection}
\label{sec:select}

At the starting path point, multiple joint configurations may satisfy the
same task constraints while leading to substantially different executable
strokes. We refer to connected portions of this task-feasible
configuration set as \emph{branches}. Fixed-pose self-motion manifolds are
a special case of this structure, and joint limits may separate feasible
solutions into disconnected components~\cite{burdick1989characterization,
luck1993self,shimizu2008analytical}.

The selection stage therefore seeks a favorable initial configuration
rather than a locally optimal posture. Candidate configurations are
obtained from the IKSel table~\cite{yuan2026iksel} by querying
$N_{\mathrm{dir}}$ cone directions, reranking the retrieved solutions,
refining them, and retaining $K$ diverse candidates through farthest-point
selection.

Because the quality of an initial configuration is determined by the
subsequent stroke, the selector is supervised directly by rollout
outcomes. Each candidate in the training set is executed with the deployed
motion-generation controller of Section~\ref{sec:traj}, and the resulting
stroke length provides its target score. A permutation-equivariant network
then ranks the candidate set using each candidate together with a
mean-pooled set context. The score is additionally conditioned on a
\emph{road table} that samples the position, tangent, and cone axis of the
upcoming path every $10$\,cm of arc length up to $1.2$\,m ahead. At deployment, selection requires only candidate generation,
one network evaluation, and refinement; no rollout is performed online.

\subsection{Physically Normalized Reactive Motion Generation}
\label{sec:traj}

After the initial configuration is selected, the trajectory is generated
incrementally. At every control interval, the task-space controller
advances the end-effector along the prescribed path, while the remaining
motion is generated in the null space of the position Jacobian. The
policy's network output must be translated into a physical null-space
velocity, and this interface determines what the learning is asked to
accomplish. We design it around two requirements: the direction should be
free of arbitrary coordinates, and the amplitude should be measured in
units the robot itself defines.

\subsubsection{Direction by exact projection}
The policy outputs a joint-space vector
$\mathbf{u}\in(-1,1)^{n}$. Its physical direction is obtained by the
orthogonal projector onto $\ker\mathbf{J}$,
\begin{equation}
\label{eq:dirproj}
\hat{\boldsymbol{\xi}}(\mathbf{q},\mathbf{u})
=
\frac{\mathbf{P}_{N}(\mathbf{q})\,\mathbf{u}}
     {\left\|\mathbf{P}_{N}(\mathbf{q})\,\mathbf{u}\right\|},
\qquad
\mathbf{P}_{N}=\mathbf{N}\mathbf{N}^{\top},
\end{equation}
where the columns of $\mathbf{N}$ span $\ker\mathbf{J}$ (computed by
singular value decomposition). The projector is unique regardless of how
the null-space basis is chosen, so the direction representation carries no
hand-constructed chart: outputs that differ only by row-space components
or by scale execute the same physical direction.

\subsubsection{Amplitude by the actuation envelope}
The largest step admissible along $\hat{\boldsymbol{\xi}}$ follows from
the per-joint velocity limits in Eq.~(\ref{eq:uq}),
\begin{equation}
\label{eq:alphafeas}
\alpha_{\mathrm{feas}}(\mathbf{q},\hat{\boldsymbol{\xi}})
=
\max
\left\{
\alpha\geq 0:
\left|
\dot{\mathbf{q}}_{\mathrm{task}}+\alpha\hat{\boldsymbol{\xi}}
\right|
\preceq
\dot{\mathbf{q}}^{\lim}
\right\},
\end{equation}
where $\dot{\mathbf{q}}_{\mathrm{task}}=\mathbf{J}^{+}\dot{\mathbf{x}}$.
Each joint contributes one linear bound, so
$\alpha_{\mathrm{feas}}$ is the minimum of $n$ closed-form ratios; no
iterative optimization is involved. The commanded fraction of this
envelope is carried by the magnitude of the projected output,
\begin{equation}
\label{eq:rho}
\rho
=
\min\left(1,\left\|\mathbf{P}_{N}\mathbf{u}\right\|\right),
\qquad
\boldsymbol{\xi}
=
\rho\,
\alpha_{\mathrm{feas}}(\mathbf{q},\hat{\boldsymbol{\xi}})\,
\hat{\boldsymbol{\xi}}.
\end{equation}
Below saturation the executed command is simply
$\alpha_{\mathrm{feas}}\mathbf{P}_{N}\mathbf{u}$: the projected network
output itself, measured in units of the feasible maximum. No tuned
amplitude bound appears anywhere in the interface; the semantics transfer
across manipulators through $\dot{\mathbf{q}}^{\lim}$ alone. Coupling the
amplitude to the output magnitude also ties commanded authority to the
policy's confidence: where the squashed output is drawn toward zero, the
commanded motion shrinks with it.

\subsubsection{Observation}
The observation $\mathbf{o}$ contains the normalized joint configuration
and its square, task vectors $\mathbf{d}$ and $\mathbf{n}$, tool-axis
features, the four normalized constraint margins of the current
configuration (joint-limit, orientation-cone, corridor, and collision
clearance, each available in closed form), three projected-gradient
magnitudes describing the local null-space leverage of the task-aligned
objectives, and the \emph{executed} previous motion
$\boldsymbol{\xi}_{t-1}/\dot{\mathbf{q}}^{\lim}$. Recording the executed
motion rather than the raw network output removes the aliasing introduced
by the projection: many outputs execute identically, and what the policy
did is otherwise unrecoverable from the current configuration alone.

\subsubsection{Training and deployment}
Proximal Policy Optimization (PPO) trains the stochastic policy
$\pi_{\theta}(\mathbf{u}\mid\mathbf{o})$ together with a critic
$\widehat{V}_{\phi}(\mathbf{o})$. The per-step reward is normalized path
progress,
\begin{equation}
\label{eq:reward}
r_t
=
\operatorname{clip}\!\left(
\frac{
(\mathbf{p}_{t+1}-\mathbf{p}_{t})^{\top}\mathbf{d}
}{
v\Delta t
},
0,1
\right),
\end{equation}
with no terminal penalty, no shaping, and no action masking; its
discounted return is the discrete-time counterpart of the exit-time
objective in Eq.~(\ref{eq:value}). The critic is used only during
training. At deployment the controller executes one network forward pass,
one null-space projection, and the $n$ scalar bounds of
Eq.~(\ref{eq:alphafeas}) per control period; no successor state is
predicted, no feasibility of the commanded action is checked, and no
candidate set is enumerated.



%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Experiments and Analysis}
\label{sec:setup}

\subsection{Experimental Setup}

Experiments are conducted on the 7-DoF xArm7, the 7-DoF Franka Research 3
(FR3), and the 6-DoF Cobotta using the \textit{One} robotic
system\footnote{https://github.com/wanweiwei07/one}, with a pen-shaped
tool attached to the end-effector. Each manipulator is evaluated on
$10{,}000$ held-out straight-line tasks specified by the starting position
$\mathbf{p}_0$, path direction $\mathbf{d}$, and task-plane normal
$\mathbf{n}$, sampled within that manipulator's own work range under a
minimum-feasibility screen; the three task sets are therefore not in
correspondence across manipulators. Training and evaluation tasks are
disjoint.

\begin{itemize}[leftmargin=1em]
\renewcommand{\labelitemi}{$\triangleright$}

    \item \uline{Task and control parameters:}
    Unless otherwise specified, the orientation-cone half-angle is
    $\theta_{\max}=30^{\circ}$, the position tolerance is
    $\epsilon_{\mathrm{pos}}=20$\,mm, the commanded speed is
    $v=0.2$\,m/s, and the control period is $\Delta t=50$\,ms. The
    proposed controller bounds its amplitude by the per-joint velocity
    limits $\dot{\mathbf{q}}^{\lim}$ (FR3: $2.175$\,rad/s for joints 1--4
    and $2.61$\,rad/s for joints 5--7); box-parameterized baselines use
    $a_{\max}=0.5$\,rad/s. All compared controllers share the same
    task-space tracking, joint-limit, orientation, and collision
    constraints.

    \item \uline{Initial configurations:}
    For the full-framework and selection experiments, the candidate pool
    contains $K=32$ configurations retrieved from
    $N_{\mathrm{dir}}=40$ directions using IKSel. The learned selector
    ranks these candidates, and the selected configuration is followed by
    one damped-least-squares refinement. Controller-only comparisons use
    the same fixed initial configuration for all methods to isolate the
    redundancy-resolution law.

    \item \uline{Classical controller:}
    The classical null-space controller uses weights $(0.8,0.4,0.2)$ for
    directional manipulability, joint-limit centering, and orientation
    regulation, respectively.

    \item \uline{Margin field:}
    The analytic constraint-margin field, used as an
    initial-configuration ranking heuristic in the selection ablation,
    uses temperature $\tau_{\mathrm{m}}=0.1$.

    \item \uline{Learned controllers:}
    The proposed reactive controller is a three-layer MLP of width
    $2048$ trained with PPO for $240$M environment steps
    ($\gamma=0.99$, $128$ parallel environments) on straight tasks; it
    deploys as one forward pass and one null-space projection per
    control period. The Continuous-PPO baseline and the policy
    inside the hybrid are trained for $30$M steps at width $512$ under
    the basis-box parameterization. At the baselines'
    matched budget and width, the proposed interface already reaches a
    mean Ratio of $84.9\%$ on FR3 straight tasks, so the comparison is
    not decided by scale alone. All resolution laws share the same
    task-space controller and constraint set.

    \item \uline{Evaluation metrics:}
    All performance is reported as the executed stroke and \emph{Ratio},
    the per-task ratio of the executed stroke to the task's numerical
    pointwise-reachable length $\ell^{\mathrm{pw}}$, reported in percent
    and summarized by its mean and 10th percentile (mean / p10). The classical controller
    appears as an ordinary row under the same metric. $\ell^{\mathrm{pw}}$
    is computed as follows. The task's path is sampled every $1$\,cm of
    arc length, and at each sample a numerical search asks whether any
    admissible configuration exists, i.e., one whose tool tip lies within
    $\epsilon_{\mathrm{pos}}$ of the sample, whose tool axis lies inside
    the orientation cone, and which respects the joint limits and
    self-collision clearance. The search projects warm starts drawn from a
    precomputed configuration table over $96$ cone-sampled tool directions
    with $16$ retries per sample, together with the previous sample's
    solution, and configurations traversed by the evaluated rollouts are
    additionally injected as feasibility witnesses;
    $\ell^{\mathrm{pw}}$ is the arc length of the first sample at which no
    admissible configuration is found. Because a failed search does not
    prove infeasibility, the reference is finally taken per task as the
    maximum of the search estimate and the executed stroke, so every
    reported Ratio is at most one by construction. With feasibility
    witnesses collected from every evaluated controller, $98\%$ of tasks
    lie within one march step of the search estimate and the median
    binding adjustment is $4$\,mm; the arc family is the exception by
    construction, since its path closes on itself and continuous
    execution may loop the circle beyond the $1.8$\,m march horizon
    (such censored tasks fall back to the executed-stroke reference).
\end{itemize}

All evaluation tables compare the four path families illustrated in
Fig.~\ref{fig:families}: straight, arc, serpentine, and rotating-axis.
The redundancy-resolution controllers and critics are trained on
straight-line tasks only, and the initial-configuration selector is
trained on straight and arc tasks; every evaluated task instance is held
out from all training. Consequently, straight and arc results measure
generalization to \emph{unseen but in-distribution} tasks, whereas
serpentine and rotating-axis results measure generalization to
\emph{out-of-distribution} (OOD) families that appear in no training set;
for the motion-generation stage, the arc family is additionally zero-shot,
since the controllers never observe curved paths during training.
Straight-line results use $10{,}000$ tasks per manipulator, and each
curved family uses $2{,}500$ tasks.

\begin{figure*}[!tbp]
    \centering
    \begin{subfigure}{0.24\textwidth}
        \centering
        \includegraphics[width=\linewidth]{imgs/path_straight_v2.jpg}
        \caption{Straight}
    \end{subfigure}\hfill
    \begin{subfigure}{0.24\textwidth}
        \centering
        \includegraphics[width=\linewidth]{imgs/path_arc_v2.jpg}
        \caption{Arc}
    \end{subfigure}\hfill
    \begin{subfigure}{0.24\textwidth}
        \centering
        \includegraphics[width=\linewidth]{imgs/path_serpentine_v2.jpg}
        \caption{Serpentine}
    \end{subfigure}\hfill
    \begin{subfigure}{0.24\textwidth}
        \centering
        \includegraphics[width=\linewidth]{imgs/path_rotating_v2.jpg}
        \caption{Rotating axis}
    \end{subfigure}
    \caption{Path families used to evaluate initial-configuration
    generalization: (a) a straight ray; (b) an arc with signed curvature;
    (c) a serpentine seam with sinusoidal lateral offset; and (d) a
    straight path with a rotating cone axis. Each panel shows the path in
    3D above the translucent task plane; red arrows indicate the local
    cone axis $\mathbf{n}(s)$, which departs from the plane normal only in
    (d). Families (a) and (b) appear in selector training, whereas (c) and
    (d) are used only for out-of-distribution evaluation.}
    \label{fig:families}
\end{figure*}

\subsection{Redundancy-Resolution Comparison}
\label{sec:ladder}

We first isolate the motion-generation stage by comparing the proposed
physically normalized reactive controller with four redundancy-resolution
baselines. For each task, all methods start from the same initial
configuration and share the same task-space controller, feasibility
constraints, and control period. Thus, differences in executed stroke are
attributable to the redundancy-resolution law.

\begin{itemize}[leftmargin=1em]
\renewcommand{\labelitemi}{$\triangleright$}

    \item \uline{Zero Null-Space (Zero):}
    No secondary motion is applied, i.e.,
    $\mathbf{a}=\mathbf{0}$. This baseline measures the stroke obtained from
    task-space tracking alone.

    \item \uline{Classical Gradient Resolution (Classical):}
    A conventional null-space gradient controller combines directional
    manipulability, joint-limit centering, and orientation regulation using
    the weights specified in Section~\ref{sec:setup}. Its command performs
    gradient ascent on the weighted secondary field,
    \begin{equation}
    \label{eq:secondary}
    \boldsymbol{\xi}
    = k_{\mu}\nabla_{\mathbf{q}} w_{\mathbf{d}}(\mathbf{q})
    - k_{\mathrm{jl}}
    \frac{\mathbf{q}-\mathbf{q}_{\mathrm{mid}}}
         {\mathbf{q}_{\max}-\mathbf{q}_{\min}}
    + k_{\theta}\, g(\theta)\,
    \nabla_{\mathbf{q}}\!\bigl(\mathbf{z}(\mathbf{q})^{\top}\mathbf{n}\bigr),
    \end{equation}
    applied as $\mathbf{a}_{\mathrm{cl}} \propto
    \mathbf{B}^{\top}\boldsymbol{\xi}$, where $w_{\mathbf{d}}$ is the
    directional manipulability along $\mathbf{d}$, the second term centers
    each joint in its range, and the gate $g(\theta)$ activates the
    orientation term as the tool axis approaches the cone boundary. It
    represents instantaneous geometric redundancy resolution without
    learned long-horizon information.

    \item \uline{Continuous-Action PPO (Continuous PPO):}
    A PPO policy over the continuous command box $[-1,1]^{m}$ with a
    tanh-squashed Gaussian head and the tanh-corrected entropy bonus,
    deployed through its deterministic mean. It measures what direct
    reactive learning achieves in hand-constructed basis coordinates.

    \item \uline{Hybrid Reactive Policy (PPO+Classical):}
    A PPO policy trained in the basis-box coordinates is
    used away from the joint-limit boundary, while the classical
    controller takes over within a thin boundary region defined by
    normalized joint-limit distance, with hysteresis thresholds
    $0.98/0.94$.

    \item \uline{Physically Normalized Reactive Policy (DirFrac, Ours):}
    The controller of Section~\ref{sec:traj}, deployed as its bare
    policy: one forward pass, one null-space projection, and the
    closed-form amplitude bound per control period. No model rollout,
    feasibility screening, or candidate enumeration is performed at
    deployment.
\end{itemize}

Table~\ref{tab:mainresult} reports the results per path family for each
manipulator; on the curved families every learned method runs zero-shot.
Classical gradient resolution provides only a modest improvement over
zero null-space motion, and the classical takeover patch of the hybrid
adds a few points over its underlying policy. The proposed controller
leads every row on the densest benchmark---$89.8\%$ mean Ratio with a
10th percentile of $63.3\%$ on FR3 straight tasks---and the lead carries
over zero-shot to the curved families it never saw during training
($83.1$, $87.7$, and $88.4$ mean Ratio on the FR3 arc, serpentine, and
rotating-axis sets).

The Continuous PPO and DirFrac rows share the same learning algorithm,
reward, and training distribution; they differ only in the action and
observation coordinates, and the gap is $69.4$ versus $89.8$ in mean
Ratio. Scale is not the explanation: at the baselines' own budget and
width the physically normalized interface already reaches $84.9\%$.
Long-horizon constraint avoidance is learnable end to end once the
interface is physically grounded.

\begin{table*}[!htbp]
\centering
\caption{Comparison of the Resolution Laws across Path Families
($10{,}000$ Straight and $2{,}500$ Tasks per Curved Family per Manipulator)}
\label{tab:mainresult}
\begin{threeparttable}
\footnotesize
\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{lllcccccc}
\toprule
& & & \multicolumn{2}{c}{Franka Research 3}
& \multicolumn{2}{c}{xArm7}
& \multicolumn{2}{c}{Cobotta} \\
\cmidrule(lr){4-5} \cmidrule(lr){6-7} \cmidrule(lr){8-9}
Type
& Method
& Family
& \shortstack{Stroke\\(m)} & \shortstack{Ratio (\%)\\mean / p10}
& \shortstack{Stroke\\(m)} & \shortstack{Ratio (\%)\\mean / p10}
& \shortstack{Stroke\\(m)} & \shortstack{Ratio (\%)\\mean / p10} \\
\midrule
\multirow{8}{*}{Classical} & \multirow{4}{*}{\shortstack[l]{Zero Null-Space}} & Straight & \pinkbox{0.308} & \pinkbox{56.8 / 16.5} & \pinkbox{0.259} & \pinkbox{55.0 / 15.7} & \pinkbox{0.159} & \pinkbox{60.0 / 25.5} \\
 &  & Arc & \pinkbox{0.318} & \pinkbox{50.4 / 11.0} & \pinkbox{0.270} & \pinkbox{50.7 / 12.4} & \pinkbox{0.160} & \pinkbox{59.3 / 25.0} \\
 &  & Serpentine & \pinkbox{0.303} & \pinkbox{54.0 / 14.9} & \pinkbox{0.258} & \pinkbox{53.1 / 14.5} & \pinkbox{0.151} & \pinkbox{56.6 / 23.4} \\
 &  & Rot.-Axis & \pinkbox{0.315} & \pinkbox{56.3 / 16.7} & \pinkbox{0.261} & \pinkbox{54.9 / 15.5} & \pinkbox{0.159} & \pinkbox{59.7 / 26.1} \\
\cmidrule(lr){2-9}
 & \multirow{4}{*}{\shortstack[l]{Classical Gradient}} & Straight & 0.332 & 60.4 / 17.9 & 0.269 & 56.4 / 17.8 & 0.163 & 61.3 / 27.6 \\
 &  & Arc & 0.345 & 53.0 / 12.1 & 0.284 & 52.1 / 13.8 & 0.164 & 60.5 / 27.1 \\
 &  & Serpentine & 0.326 & 57.4 / 16.4 & 0.268 & 54.6 / 16.3 & 0.156 & 57.8 / 25.0 \\
 &  & Rot.-Axis & 0.339 & 59.9 / 18.4 & 0.273 & 56.7 / 17.9 & 0.163 & 61.0 / 28.3 \\
\midrule
\multirow{12}{*}{Learned} & \multirow{4}{*}{\shortstack[l]{Continuous PPO}} & Straight & 0.408 & 69.4 / 17.4 & 0.340 & 66.7 / 16.8 & 0.197 & 71.1 / 30.3 \\
 &  & Arc & 0.447 & 61.5 / 13.0 & 0.380 & 62.2 / 14.4 & 0.201 & 70.5 / 30.7 \\
 &  & Serpentine & 0.407 & 67.0 / 16.0 & 0.337 & 64.5 / 16.1 & 0.191 & 67.9 / 27.3 \\
 &  & Rot.-Axis & 0.411 & 68.4 / 17.4 & 0.344 & 66.9 / 17.2 & 0.199 & 71.0 / 30.9 \\
\cmidrule(lr){2-9}
 & \multirow{4}{*}{\shortstack[l]{PPO + Classical}} & Straight & 0.458 & 75.7 / 23.6 & \limebox{0.374} & \limebox{71.5 / 20.9} & \limebox{0.207} & \limebox{74.1 / 33.8} \\
 &  & Arc & 0.516 & 67.4 / 16.7 & \limebox{0.422} & \limebox{67.0 / 17.4} & \limebox{0.213} & \limebox{74.0 / 34.7} \\
 &  & Serpentine & 0.457 & 73.0 / 21.4 & \limebox{0.370} & \limebox{69.2 / 19.6} & \limebox{0.202} & \limebox{71.2 / 31.3} \\
 &  & Rot.-Axis & 0.458 & 74.4 / 23.3 & \limebox{0.374} & \limebox{71.3 / 20.5} & \limebox{0.209} & \limebox{74.2 / 35.2} \\
\cmidrule(lr){2-9}
 & \multirow{4}{*}{\shortstack[l]{\textbf{DirFrac Reactive (Ours)}}} & \textbf{Straight} & \limebox{\textbf{0.598}} & \limebox{\textbf{89.8 / 63.3}} & \textbf{[XX]} & \textbf{[XX]} & \textbf{[XX]} & \textbf{[XX]} \\
 &  & \textbf{Arc} & \limebox{\textbf{0.764}} & \limebox{\textbf{83.1 / 35.6}} & \textbf{[XX]} & \textbf{[XX]} & \textbf{[XX]} & \textbf{[XX]} \\
 &  & \textbf{Serpentine} & \limebox{\textbf{0.600}} & \limebox{\textbf{87.7 / 58.3}} & \textbf{[XX]} & \textbf{[XX]} & \textbf{[XX]} & \textbf{[XX]} \\
 &  & \textbf{Rot.-Axis} & \limebox{\textbf{0.589}} & \limebox{\textbf{88.4 / 54.5}} & \textbf{[XX]} & \textbf{[XX]} & \textbf{[XX]} & \textbf{[XX]} \\
\bottomrule
\end{tabular}
\begin{tablenotes}
    \item[Note 1] All methods share the same per-task initial configuration.
    Ratio is the per-task ratio of the executed stroke to the task's
    pointwise-reachable length $\ell^{\mathrm{pw}}$, reported in percent
    as mean / p10.
    \item[Note 2] The proposed controller is trained per manipulator on
    straight tasks; [XX] entries await the corresponding replication
    runs.
\end{tablenotes}
\end{threeparttable}
\end{table*}

\paragraph{Full framework.}
We next combine the learned initial-configuration selector with a fixed
motion-generation controller. Table~\ref{tab:framework} reports the
result on the FR3 evaluation sets of all four path families, with the
motion controller identical across all rows so that only the initial
configuration changes. On straight tasks, learned selection increases the
mean stroke from $0.642$\,m for a randomly chosen candidate to
$0.682$\,m, reaching a mean Ratio of $95.6\%$ (10th percentile $95.2\%$),
while the within-pool oracle indicates the remaining gap attributable to
candidate ranking; the same ordering holds on every curved family.

\begin{table}[!htbp]
\centering
\caption{Full-Framework Results on the FR3 Evaluation Sets
(One Fixed Motion-Generation Controller in All Rows)}
\label{tab:framework}
\begin{threeparttable}
\begin{tabular}{llcc}
\toprule
Initial configuration & Family
& \shortstack{Stroke\\(m)}
& \shortstack{Ratio (\%)\\mean / p10} \\
\midrule
\multirow{4}{*}{Random candidate}
& Straight   & 0.642 & 90.6 / 79.3 \\
& Arc   & 0.783 & 84.7 / 55.5 \\
& Serpentine   & 0.602 & 88.2 / 73.8 \\
& Rot.-Axis   & 0.594 & 89.3 / 73.2 \\
\midrule
\multirow{4}{*}{\textbf{Learned selector (ours)}}
& \textbf{Straight}   & \textbf{0.682} & \textbf{95.6 / 95.2} \\
& \textbf{Arc}   & \textbf{0.876} & \textbf{89.9 / 50.8} \\
& \textbf{Serpentine}   & \textbf{0.634} & \textbf{92.3 / 87.7} \\
& \textbf{Rot.-Axis}   & \textbf{0.632} & \textbf{93.7 / 90.5} \\
\midrule
\multirow{4}{*}{Within-pool oracle ($K$ rollouts)}
& Straight   & 0.714 & 98.9 / 98.2 \\
& Arc   & 1.014 & 95.9 / 93.8 \\
& Serpentine   & 0.684 & 97.1 / 94.4 \\
& Rot.-Axis   & 0.680 & 98.5 / 98.0 \\
\bottomrule
\end{tabular}
\begin{tablenotes}
    \item[Note 1]
    $\ell^{\mathrm{pw}}$ denotes the numerical pointwise-reachability
    reference used for task-wise normalization. This table uses the FR3
    benchmark set for which $\ell^{\mathrm{pw}}$ is available; its task
    distribution differs from that of Table~\ref{tab:mainresult}, so
    absolute strokes are not directly comparable across the two tables.
\end{tablenotes}
\end{threeparttable}
\end{table}

\subsection{Ablation Study}

\subsubsection{Initial Configuration Selection}
\label{sec:branch}
\label{sec:ablate_seed}

To isolate the selection stage, we fix the motion-generation controller and
compare different ranking rules on the same candidate pool.
Table~\ref{tab:ablate_seed} reports results on $2500$ held-out tasks per
path family. On straight tasks, taking the first feasible candidate
reaches a mean Ratio of $89.6\%$;
ranking by joint-limit margin or by the analytic margin field
\begin{equation}
\label{eq:margin-field}
\Phi_{\mathrm{m}}(\mathbf{q})
=
-\tau_{\mathrm{m}}
\log
\sum_{j}
\exp\left(
-m_{j}(\mathbf{q})/\tau_{\mathrm{m}}
\right),
\end{equation}
where $m_j$ denotes a normalized joint-limit or orientation-cone margin,
raises this to $94.3\%$, while the learned selector reaches $94.8\%$ with a 10th
percentile of $93.4\%$. These results show that rollout supervision provides a
stronger ranking signal for initial configurations than the tested
instantaneous geometric heuristics.

\begin{table*}[!htbp]
\centering
\caption{Comparison of Initial Configuration Selection Methods on $2500$
Held-Out FR3 Tasks per Path Family}
\label{tab:ablate_seed}
\begin{threeparttable}
\footnotesize
\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{lllccc}
\toprule
Type
& Candidate Ranking Rule
& Family
& \shortstack{Stroke\\(m)}
& \shortstack{Ratio (\%)\\mean / p10}
& \shortstack{Near-Oracle\\Tasks (\%)} \\
\midrule
 \multirow{4}{*}{\textemdash} & \multirow{4}{*}{\shortstack[l]{First Feasible Candidate}} & Straight & \pinkbox{0.608} & \pinkbox{89.6 / 61.4} & 58.6 \\
  &  & Arc & \pinkbox{0.764} & \pinkbox{83.2 / 35.7} & 55.0 \\
  &  & Serpentine & \pinkbox{0.600} & \pinkbox{87.8 / 58.3} & 63.2 \\
  &  & Rot.-Axis & \pinkbox{0.589} & \pinkbox{88.4 / 54.5} & 62.4 \\
\midrule
 \multirow{4}{*}{Random} & \multirow{4}{*}{\shortstack[l]{Random Valid Candidate}} & Straight & 0.611 & 90.5 / 76.8 & \pinkbox{22.1} \\
  &  & Arc & 0.783 & 84.7 / 55.5 & \pinkbox{19.7} \\
  &  & Serpentine & 0.602 & 88.2 / 73.8 & \pinkbox{25.2} \\
  &  & Rot.-Axis & 0.594 & 89.3 / 73.2 & \pinkbox{25.0} \\
\midrule
 \multirow{16}{*}{Heuristic} & \multirow{4}{*}{\shortstack[l]{Max Directional Manipulability}} & Straight & 0.616 & 90.4 / 66.9 & 60.6 \\
  &  & Arc & 0.778 & 84.0 / 36.1 & 56.9 \\
  &  & Serpentine & 0.602 & 87.8 / 58.7 & 64.6 \\
  &  & Rot.-Axis & 0.600 & 89.4 / 60.0 & 64.8 \\
\cmidrule(lr){2-6}
  & \multirow{4}{*}{\shortstack[l]{Max Orientation Margin}} & Straight & 0.614 & 90.7 / 66.3 & 61.6 \\
  &  & Arc & 0.791 & 84.8 / 39.0 & 57.8 \\
  &  & Serpentine & 0.606 & 88.5 / 61.4 & 65.3 \\
  &  & Rot.-Axis & 0.596 & 89.5 / 63.7 & 65.0 \\
\cmidrule(lr){2-6}
  & \multirow{4}{*}{\shortstack[l]{Max Joint-Limit Margin}} & Straight & 0.643 & 94.1 / 87.0 & 69.9 \\
  &  & Arc & 0.842 & 88.9 / 48.1 & 68.4 \\
  &  & Serpentine & 0.637 & 92.3 / 82.6 & 75.7 \\
  &  & Rot.-Axis & 0.629 & 93.3 / 82.4 & 75.0 \\
\cmidrule(lr){2-6}
  & \multirow{4}{*}{\shortstack[l]{Margin Field $\Phi_{\mathrm{m}}$}} & Straight & 0.644 & 94.3 / 87.5 & 69.9 \\
  &  & Arc & 0.844 & 89.1 / 50.1 & 68.8 \\
  &  & Serpentine & \limebox{0.638} & \limebox{92.5 / 83.0} & 76.2 \\
  &  & Rot.-Axis & 0.629 & 93.4 / 82.6 & 75.1 \\
\midrule
 \multirow{4}{*}{Learned} & \multirow{4}{*}{\shortstack[l]{\textbf{Learned Selector (Ours)}}} & \textbf{Straight} & \limebox{\textbf{0.648}} & \limebox{\textbf{94.8 / 93.4}} & \limebox{\textbf{75.7}} \\
  &  & \textbf{Arc} & \limebox{\textbf{0.876}} & \limebox{\textbf{89.9 / 50.8}} & \limebox{\textbf{72.9}} \\
  &  & \textbf{Serpentine} & \textbf{0.634} & \textbf{92.3 / 87.7} & \limebox{\textbf{80.7}} \\
  &  & \textbf{Rot.-Axis} & \limebox{\textbf{0.632}} & \limebox{\textbf{93.7 / 90.5}} & \limebox{\textbf{80.6}} \\
\midrule
 \multirow{4}{*}{\textemdash} & \multirow{4}{*}{\shortstack[l]{Within-Pool Oracle ($K$ Rollouts)}} & Straight & 0.687 & 98.5 / 98.6 & 100.0 \\
  &  & Arc & 1.014 & 95.9 / 93.8 & 100.0 \\
  &  & Serpentine & 0.684 & 97.1 / 94.4 & 100.0 \\
  &  & Rot.-Axis & 0.680 & 98.5 / 98.0 & 100.0 \\
\bottomrule
\end{tabular}
\begin{tablenotes}
\item[Note 1] All methods rank the same candidate pool and use the same
motion-generation controller. Ratio is the per-task ratio of the achieved
stroke to the task's pointwise-reachable length $\ell^{\mathrm{pw}}$,
reported in percent as mean / p10. Near-Oracle Tasks denotes the percentage of tasks
whose achieved stroke is within $1$\,cm of the within-pool oracle stroke.
\end{tablenotes}
\end{threeparttable}
\end{table*}


\subsection{Generalization and Applications}

\subsubsection{Path-Type Generalization}
\label{sec:ood}

We summarize how the learned initial-configuration selector transfers
across path families. The selector is trained on $20{,}000$ tasks,
comprising $10{,}000$ straight and $10{,}000$ arc tasks in equal
proportion, with all $K$ candidates of every training task rolled out to
produce the ranking labels; it is then evaluated on $2{,}500$ held-out
tasks from each of the four families. The Learned Selector rows of
Table~\ref{tab:ablate_seed} distinguish unseen in-distribution tasks
(straight, arc) from OOD tasks belonging to the serpentine and
rotating-axis families that are absent from training. The results show that the achieved Ratio is governed by path curvature
rather than by training membership: the OOD rotating-axis family, whose
centerline is straight, matches the in-distribution straight family
($93.7\%$ vs $94.8\%$ in mean Ratio), and the OOD serpentine family even
exceeds the in-distribution arc family ($92.3\%$ vs $89.9\%$). On every family the learned
selector remains the strongest non-oracle ranking rule.




\subsubsection{Implication for Manipulator Base Placement}
\label{sec:place}

Continuous kinematic reachability also induces a task-aware criterion for
manipulator base placement: for a candidate base pose, the required path
is transformed into the robot frame and evaluated with the same
continuous-path formulation, and the pose is feasible for the task if the
complete path can be executed as a single continuous stroke. Unlike
waypoint-wise reachability, this criterion accounts for the continuity of
the joint trajectory and the choice of initial configuration, and
therefore distinguishes poses from which the path is continuously
executable from those whose points are merely individually reachable.


\subsection{Real-World Validation}
\label{sec:real}

Because the proposed framework is kinematic, the physical experiments are designed to assess whether the generated extreme motions remain executable after accounting for hardware motion limits. Trajectories are generated offline by the learned selector and the deployed motion-generation controller, then time-parameterized using Ruckig~\cite{berscheid2021jerk} under the manufacturer's joint velocity and acceleration limits. The resulting trajectories are executed on a Franka Research 3 through its joint-position interface.

We evaluate $[N]$ tasks spanning a range of simulated stroke lengths and compare simulation and hardware in terms of completed stroke and Cartesian path-tracking error. The hardware reproduces $[XX]\%$ of the simulated stroke on average, with an RMS / maximum path deviation of $[XX]/[XX]$\,mm. These results show that the kinematically generated motions remain executable after time parameterization and can be reproduced on the physical robot while maintaining the prescribed Cartesian path.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Conclusions and Future Work}
\label{sec:conclusion}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

In this paper, we studied \textit{continuous kinematic reachability}, the maximum extent of a prescribed path that can be realized by a single continuous joint trajectory. Extreme motion generation was formulated as two coupled decisions: selecting the initial configuration and resolving redundancy throughout execution. We evaluated the realized stroke across three manipulators and used numerical pointwise reachability as a task-specific reference.

For redundancy resolution, the deciding factor proved to be the coordinates of the action and observation interface. With the direction given by the exact null-space projector, the amplitude anchored to the per-joint velocity limits, and the observation exposing executed motion and constraint margins, a plain policy-gradient run deploys as a single forward pass per control period and becomes the strongest resolution law in our evaluation. For initial configuration selection, rollout-supervised ranking further improves the achievable stroke and transfers to path families absent from selector training.

Together, these results indicate that long-horizon constraint avoidance is learnable end to end once the interface is physically grounded. The same continuous-path formulation also induces a task-aware criterion for manipulator base placement. Future work will extend the framework to dynamic constraints and paths specified online.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\normalem
% \bibliographystyle{IEEEtran}
\bibliographystyle{IEEEtranN}
\bibliography{citations.bib}

% \appendix

% \section{Segmentation Results with One-shot on the In-rack Test Tubes}

% Fig. \ref{global_seg} shows the segmentation result on the rack by SAM with different model capacities. We can find that the segmented masks include much regions with no interest, such as platform, slot, and lower section of the test tube. The accuracy of global segmentation for cap detection is suboptimal, and it also suffers from the issue of duplicate detections.

% \begin{figure}[h]
% 	\centering
% 	\includegraphics[width=\linewidth]{imgs/global_seg.jpg}
%     \caption{\tang{Segmentation results with one-shot:} (a) Global image. (b) Segmented masks with \(16\times16\) sampling points. (c) Segmented masks with \(32\times32\) sampling points. (d) Segmented masks with \(64\times64\) sampling points.}
% 	\label{global_seg}
% \end{figure} 

\end{document}