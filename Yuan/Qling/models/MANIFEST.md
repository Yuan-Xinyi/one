# Qling 已验证可完成任务的策略存档（2026-08-31）

| 文件 | 任务 | 验证成绩（真起点/修正口径） | 环境 kind / obs / act |
|---|---|---|---|
| v0_30M_agent.pt | 点物体桌面拖拽（10k 任务分布） | 92.2% placed / 89.1% success（可行性天花板 ~94.5%） | drag / 27 / 7 |
| cont_v2_10M_agent.pt | 30×20 容器 180° 调头（0 换抓） | **100%**（256/256，13 抓取全绿） | container / 35 / 7 |
| rg50_ctd_agent.pt | 50×40 原始尺寸调头（1 换抓，slot_y=0.40） | **100%**（512/512，全部经计划换抓） | regrasp / 37 / 8 |
| rg45_8k_v2_agent.pt | 45×35 调头（剧本换抓版） | 63.3%（成功全经换抓；残余失败=出工作区） | regrasp / 37 / 8 |
| bottle_v3_10M_agent.pt | compare_exp 躺瓶 SE(2) 全位姿（20 抓取候选） | **100%**（critic 选抓取 6/8；~4M 步中期快照，训练仍在续） | bottle / 35 / 7 |

- 配套 `*_config.yaml` 为各自训练时的完整解析配置（复现入口：`python -m drag.train_drag --config <yaml>`）。
- 加载评测：见 drag/eval_container.py、drag/autopsy_regrasp.py、drag/eval_final.py 的 Agent 构造方式（hidden 512，squashed_entropy）。
- 注意：bottle/regrasp_free 类环境评测须用 2026-08-31 修复后的代码（倾角-方位保持、候选主序选择器、start_mode 生效）。
- 待补：rg45f（自由 regrasp）训练中，收官后追加。

## slope15_1M / slope45_1M（瓶子平面→斜面，2026-09-04）
- 场景：折线 y=0.48 平行 x 轴，坡角 15°/45°（cfg slope_theta_deg），瓶长轴沿 x，从 y=0.40 推上坡 7cm；纯平移目标。
- 环境 drag/bottle_slope_env.py：约束行钩子化（_constraint_rows/_hold_targets），桥跨闭式场（v_z−c·v_y、ω_x−k·v_y 耦合行），过坡漂移 ≤1mm/0.07°。
- slope15_1M：1M 步即 eval 100%（首个 eval 点）。
- slope45_1M：真起点终评 **255/256=99.6%**，中位 1.6s；critic 选抓取 1(颈)×234 + 8×22。注意：该任务续训会退化（4M 后 1.0→0.69，ws 死亡出现），1M 快照即终稿——"退火尾段才见分晓"规律不适用于秒收敛政权。
- 视频 slope45_rl_side.mp4 / 解析 slope45_analytic_side.mp4；渲染器 drag/render_bottle_slope.py（侧视标准机位）。

## slope45rg_30M（45° 坡随机目标·目标条件化，2026-09-04）
- 目标随机：位置连续横跨平面→坡面（桥跨带拒绝）、x ±12cm、朝向 ±45°；起点-目标抓取相容过滤保证任务恒有解。
- 新机械：二维桥跨场 f(y,ψ)（对角双棱有效半宽）、局部坡面系方位自由倾角保持（坡上原地转体实测 54°）。obs 39 维。
- 512 随机目标终评（seed 91）：**97.3%**，坡上 98.4%、坡上|yaw|>25° 98.0%、平面 96.6%；中位 1.1s。终点权重优于中途最优快照（本政权无退化）。
- 视频 slope45rg_showcase.mp4（坡上+35°目标）。

## slope45any_60M（45° 坡任意→任意通用搬运策略，2026-09-04）
- 起点与目标都随机：位置横跨平面/坡面（桥跨带拒绝）、朝向 ±60°；起点-目标抓取相容三层保底（重采样→平地缺省→v1 锚点，锚可行集 __init__ 预解）。
- 512 任务四象限终评（seed 91）：**总 97.3%** — 平→平 99.5 / 平→坡 96.9 / 坡→平 93.3 / 坡→坡 98.6；中位 ~1.4s。
- 30M 版 agent_best（95.7%）同栏留档 slope45any_30M；60M 终点胜出。
- 视频 slope45any_showcase.mp4（坡上起点→平面目标，全程转向 110°）。

## hill_v2_60M（翻山任意→任意：桌→45°坡→8cm高台，2026-09-05）
- 通用地形框架首个应用：terrain.py 静置族数值求解器（坡角包络限制 argmin），凸折线翻沿族连续；场陡度限速器（K_KNEE 14 二次律）+ 投影迭代钳制 ±0.03rad 治手臂跳变（32°→8.75°）。
- 512 任务四象限（seed 91）：**总 95.9%** — 桌→桌 .994 / 桌→台 .983 / 台→台 .971 / 台→桌 .828；穿透 p99 0.052mm；精度 0.4mm/1.2°；零速度违规。
- 高台可达带：凸沿后 8cm 内（20 抓取中 2-4 个可行）。视频 hill_up/down.mp4。
