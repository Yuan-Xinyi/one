# `one.camera`

Prefer these in-house utilities over trimesh / scipy / open3d / fcl. Auto-generated by `tools/gen_api_index.py` (static ast scan); re-run after adding functions.

## `one.camera.RS435.detect_cube`
_Detect a cube on the table from the calibrated D435, in the xArm7 BASE frame._

- `load_extrinsics(path=DEFAULT_YAML)` — Load ``T_base_cam`` (4x4) and the colour intrinsics dict from the yaml.
- `apply_T(T, pts)` — Apply a 4x4 homogeneous transform to (N,3) points.
- `capture_base_cloud(T_base_cam, z_near=0.15, z_far=2.0)` — Grab one D435 cloud and return ``(pts_base(N,3), colors(N,3))`` in the base
- `estimate_table_z(pts, bins=80)` — Table height = the densest z band of the cropped cloud (the table is the
- `detect_cube_base(pts_base, colors, cube_size=CUBE_SIZE, workspace=WORKSPACE, table_z=None, target_rgb=None)` — Find the cube in a base-frame cloud.

## `one.camera.RS435.register_camera_extrinsics`
_D435 外参标定:把真实点云与仿真 xArm7+XHand 点云做交互式配准。_

- `capture_realsense(z_min, z_max, voxel=0.004, warmup=15)` — 拍一帧,返回 (pts(N,3) color光学系, colors(N,3) 0-1, intrinsics dict)。
- `read_arm_jnts_rad(ip)`
- `connect_hand(port, baud)` — 连接真实 XHand(dex-hand 自带 one/control/end_effector/xhand 的 RS-485 驱动)。
- `command_zero_read_measured(hand)` — 把 12 个手指命令到全 0(张开),并回读它们的**实测**关节角。
- `sim_cloud(jnts_rad, hand_rad=None, n=20000)` — 子进程调 wrs 环境生成仿真机械臂表面点云(base 系)。
- `load_init_T(path)`
- `rough_overhead_guess()` — 无任何先验时的粗略俯视相机外参:base 前方 0.6 m、高 0.8 m,朝下看。
- `rot_about(axis, ang, center)` — 绕过 center、方向为 axis 的轴转 ang 的 4x4 齐次矩阵。
- `apply_T(T, pts)`
- `fmt_T(T)`
- `main()`
- **class `RegisterApp`** — 反向配准:相机点云用 T_view 冻结显示,键盘移动机械臂(位姿 M),
  - methods: `sim_hand_pose`, `acquire`, `robot_in_view`, `T_base_cam`, `centroid_align`, `icp_refine`, `save`, `verify_report`, `run`, `refresh`

## `one.camera.RS435.sim_xarm7_xhand_cloud`
_Sample a surface point cloud of the simulated xArm7 + XHand at a given joint_

- `sample_robot_cloud(jnts_rad, hand_rad=None, n_total=20000)`
- `main()`
